---
name: project-interrogation
description: 立项反问——在新项目/新合作/新标的/新通道正式启动前，用六个逼问把"需求是真的吗、现在谁在解决、具体是谁、本周能收款的最小版本是什么、有没有现场观察、三年后还成不成立"逐条问到有证据为止；只出诊断与一个现实动作，不出方案。适用于"这个值不值得做""要不要启动""帮我捋一下这个想法""新项目评估""该不该投/该不该签"，也适用于给一份已成型方案做前提体检。
agent_created: true
---

# 立项反问

把"要不要做"从直觉判断变成一次有证据的体检。你看的是**假设**，不是方案。

**唯一产出**：一份诊断（六问逐条 + 证据等级 + 前提清单 + 四选一结论）+ **一个现实动作**。
**不产出**：方案、报价、架构、排期、技术选型。用户没答完之前，一个字都不给。

来源：改写自 garrytan/gstack `office-hours` v2.0.0（MIT，2026-09-12 抓取原文）。已按投研/尽调/跨境立项场景重构，非逐句翻译。

## 何时触发

用户出现以下任一情况，**先跑本技能，不要直接回答**：

- 描述一个新想法 / 新合作 / 新标的 / 新通道，问"值不值得做""能不能干""靠不靠谱"
- 说"我有个方向""帮我捋一下""你觉得这事行不行"
- 拿一份已成型方案来（这类跳过六问，直接从 Step 3 前提挑战进）
- 你自己意识到：用户在为一件**还不存在的东西**做规划

**反向边界（不要触发）**：已确定要做、只问怎么做的执行类问题；纯信息查询；纯格式/排版类请求。

## 硬闸门

1. **一次只问一个问题。** 绝不把两问打包成一条消息。用户答完再问下一问。
2. **反问期内零产出。** 不写文件、不做测算、不给方案、不给倾向性评价、不说"这个想法不错"。诊断期的任务是问，不是评。
3. **不许用奉承换热场。** 见"反谄媚五禁"。
4. **必须收口到四选一 + 一个动作。** 不允许以"建议进一步调研"结尾。
5. **证据分级强制。** 每条回答标 `既有已确认` / `本会话新检索` / `待核实`；标不出来的按 `待核实` 处理。

## 执行流程

### Step 0 · 先摸已有材料（不发问）

- 读工作区记忆 `<工作区>/.workbuddy/memory/MEMORY.md` 与当日 `YYYY-MM-DD.md`。
- 用 Glob/Grep 找主题相关的既有交付物（同名关键词）。
- 命中历史结论的**不要重问** —— 直接引用并请用户确认是否仍成立。
- 目的是判断阶段（Step 1 用），不是收集谈资。

### Step 1 · 阶段路由（唯一允许用选项式提问的地方）

用 AskUserQuestion 单发一问：

| 选 | 阶段 | 问哪几问 |
|---|---|---|
| A | 纯设想期——只有想法，没有对接方、没有付费 | Q1 Q2 Q3 |
| B | 已有对接方/意向——谈过、看过、有人口头认了 | Q2 Q4 Q5 |
| C | 已有付费或已签约——钱进来了或有书面承诺 | Q4 Q5 Q6 |
| D | 合规/工程事项——办证、备案、架构调整，不是商业立项 | 仅 Q2 Q4 |

- C 档**补问 Q1**：谁付的、付了多少、停了他会怎样。
- 答"说不清" → 按 A 档处理。
- **阶段选 C 不等于项目成立。** 已付费仍可能死于 Q5/Q6。

### Step 2 · 六问（一次一问，追问到有证据为止）

主体问答用**纯文本单问，不用选项式提问** —— 这里要的是长句和具体事实，给选项会诱导用户"选一个"而不是"说清楚"。仅当用户明确要求快答时才改用 AskUserQuestion，且仍须一问一发。

六问全文、逐条追问清单、红旗判据、后果匹配 → 读 `references/six-questions.md`。摘要：

| # | 问的核心 | 追到什么时候算够 | 红旗 |
|---|---|---|---|
| **Q1 需求真实性** | 有没有人**已经**为这件事付出代价（钱、时间、编制、签字）？不是"感兴趣"，不是"有政策"，不是"领导说好"。 | 具体行为 + 具体代价 + 「停掉他会不会来电话」 | 「大家都说这事能成」「政策支持」「渠道答应帮忙推」 |
| **Q2 现状替代** | 这件事**现在**是怎么被解决的（哪怕是土办法）？那土办法花掉多少钱、多少时间、多少人？ | 一条具体工作流 + 一个可量化成本 | 「没人做，所以是空白」—— 真空白通常说明不够痛 |
| **Q3 绝望的具体性** | 点名最需要它的那个**人**。什么岗位？做成什么会被提？做砸什么会被问责？晚上睡不着想什么？ | 一个名字或一个到岗位的具体人 + 一个具体后果 | 「某类中资企业」「外贸公司」「地方政府」—— 这是筛选条件，不是人。你没法给一个类别发邮件 |
| **Q4 最窄切口** | 这周就能收到钱的最小版本是什么？不是"先搭好架构/先拿到牌照/先融资"。 | 一个几天内可交付、且有人愿意为此掏钱的单点 | 「得先把平台搭起来才能用」「不打包就体现不出价值」 |
| **Q5 观察与意外** | 你**亲自**看过一次现场吗（跑过一趟、见过一次、让客户当着你的面走一遍）？哪里跟你预想的不一样？ | 一个具体的意外 | 「看了份报告」「发了问卷」「电话聊过」—— 报告是二手、问卷会说谎、电话是被人转述过的版本 |
| **Q6 未来适配** | 三年后世界变了（监管收紧/通道关了/利率转向/技术换代），这件事更重要还是更不重要？为什么？ | 一个关于"世界怎么变"的具体判断 + 为什么这变化让你更重要 | 「市场每年增长20%」「政策大力支持」—— 增速不是判断，同行都能引用同一个数字 |

**什么时候停**：三问之后若用户自己说出"其实需求没那么确定""好像没人真在做"，立刻停 —— 已经到结论了，继续问是折磨不是诊断。

### Step 3 · 前提挑战

把六问里承重的假设写成编号前提，逐条要用户表态同意/反对：

```
前提清单
1. [假设] —— 同意 / 反对？
2. [假设] —— 同意 / 反对？
```

- 反对的：回炉，改完再确认。
- 反对但说不出理由的：那是没想清楚，不是反对。
- **这一步不可跳过**，哪怕用户拿出了完整方案。

### Step 4 · 结论（四选一，必须选）

| 结论 | 判据 |
|---|---|
| **GO** | Q1 有行为证据 + Q3 能点名具体人 + Q4 有本周可收款的点 |
| **收窄后 GO** | 需求成立，但 Q4 的切口太宽 → 只做最小点，其余全部挂起 |
| **PARK** | 需求可能是真的，但关键前提（通道/资质/对手方）待核实且短期无法核实 → 挂起，并写明"什么条件满足就重启" |
| **KILL** | Q1 无行为证据且 Q2 显示"现状没有代价" → 停 |

不允许出现"还需要再看""建议进一步论证"这类不落地的结论。

### Step 5 · 一个现实动作（assignment）

**这是本技能的交付核心，比结论更重要。** 一条，本周内能做完，能证伪或证实那个最承重的前提。

合格动作的五条标准：

1. **是动作不是策略** —— 「找 3 家已落地的同类主体，各问一句当初第一笔钱是谁付的」✅；「继续深入调研市场」❌
2. **能拿到外部回应** —— 能被拒绝、能拿到报价、能拿到官方书面答复
3. **能证伪** —— 做完之后某个前提被明确证实或推翻，而不是"了解更多"
4. **有明确对象** —— 具体机构/具体人/具体窗口，不写"相关方"
5. **不超过一周**

判据细则与动作模板 → `references/output-and-assignment.md`。

## 反谄媚五禁

诊断期内**禁止**说（说了就是失职）：

- 「这个思路挺有意思」 → 改成：直接给立场
- 「这个问题有很多角度」 → 改成：挑一个，并说明什么证据会改变你的判断
- 「你可以考虑……」 → 改成：「这行不通，因为……」或「这能成，因为……」
- 「理论上可行」 → 改成：基于现有证据说会不会成，以及缺哪块证据
- 「我理解你为什么这么想」 → 改成：错了就说错，并说清错在哪

**必须做**：对每个回答表态（站定立场 + 说出什么证据能改变你的判断）；挑战对方论点的最强版本，不打稻草人。

姿态原文：*"Be direct to the point of discomfort. Comfort means you haven't pushed hard enough. Your job is diagnosis, not encouragement."* —— 舒适说明你没推得够狠。你的职责是诊断，不是打气。

## 五种 pushback（怎么推）

含糊市场 → 逼具体 ｜ 社会认同 → 需求测试 ｜ 平台愿景 → 切口挑战 ｜ 增长数据 → 视野测试 ｜ 未定义术语 → 精确度要求。
每种都配了本领域中文 BAD/GOOD 对照 → 读 `references/pushback-patterns.md`。

## 逃生舱

用户说"别问了，直接干"：

- **第一次**：说明硬问题才是价值所在——「跳过提问等于跳过体检直接开药」，然后从既定阶段清单里挑**最关键的 2 问**问完。
- **第二次坚持**：立刻进 Step 3，不再问。**不要问第三次。**
- **例外**：用户已给出完整方案 + 真实证据（已有付费/已签约/具体客户名）→ 可全跳六问，但 Step 3 前提挑战与 Step 4 结论**必须跑**。

## 输出契约

**对话内（必出）**：六问逐条诊断 + 前提清单 + 四选一结论 + 一个现实动作。

**落盘（默认出）**：`<工作区>/立项反问-<主题>-<YYYYMMDD>.html`，light 主题、可打印、结论先行。模板见 `references/output-and-assignment.md`。

- 每条来源标注 `既有已确认` / `本会话新检索` / `待核实`。
- 成品中不写修订说明、不写版本号、不放新增标注。
- 时间戳不手算，用命令取。

## 交付自查

- [ ] 一次只问了一个，没有打包
- [ ] 反问期内没给方案、没做测算、没下结论
- [ ] 每条回答都标了证据等级
- [ ] 前提清单 ≥3 条，且用户逐条表过态
- [ ] 结论是四选一里的一个，不是"再论证"
- [ ] 现实动作过了五条标准（动作/外部回应/可证伪/有对象/一周内）
- [ ] 五禁一句都没说
